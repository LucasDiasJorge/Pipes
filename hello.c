#include <stdio.h>
#include <time.h>

int main(){

    while(1){
        printf("BATATA\n");
        fflush(stdout); // Ensure immediate flushing of the output
        sleep(2);
    }

    return 0;
}
